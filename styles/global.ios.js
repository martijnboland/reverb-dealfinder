import { StyleSheet } from 'react-native'
import { colors as sharedColors, styles as sharedStyles } from './_global';

export const colors = sharedColors;
export const styles = StyleSheet.create(sharedStyles);